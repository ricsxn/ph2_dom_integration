#km3base
Base CentOS7 docker image enriched with the basic set of mandatory software packages.
This image is used by km3test image.

To build this image use the command:

`docker build -t km3/km3base:1.0 .`